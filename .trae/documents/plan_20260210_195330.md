I will modify the following files to translate all user-facing log messages into Chinese (Simplified):

1.  **`main.py`**: Translate task status logs (start, stop, success, failure, delay, etc.).
2.  **`core/search.py`**: Translate search process logs (keyword searching, results found, timeouts, extraction stats).
3.  **`core/comment.py`**: Translate comment action logs (navigation, scrolling, image upload steps, comment typing, button clicking, validation).
4.  **`core/auth.py`**: Translate authentication logs (loading cookies, QR login instructions, login status checks).
5.  **`gui.py`**: Translate GUI-specific logs (task running, stopping, backend status).

This will ensure that the "software displayed log" is fully localized, as requested. The local log files will also reflect these changes, which is acceptable per the user's instructions.