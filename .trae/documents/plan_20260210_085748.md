# 修复 Playwright 浏览器缺失问题

您遇到的错误提示 `Executable doesn't exist` 表明 Playwright 尚未下载所需的浏览器内核（Chromium）。这是首次使用 Playwright 时的常见步骤。

## 修复步骤
1.  **安装浏览器内核**：运行 `playwright install` 命令，这将自动下载适合您系统的 Chromium、Firefox 和 WebKit 浏览器。
2.  **验证修复**：安装完成后，再次运行 `python main.py` 启动程序。

我将立即为您执行这些操作。