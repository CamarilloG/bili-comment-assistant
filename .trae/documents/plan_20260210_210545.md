I have researched the issue. The current `SearchManager` only scrapes the first page of results. When strict filtering (Date/Title) is enabled, it's possible that all videos on the first page are filtered out, causing the task to end prematurely even if the target comment count isn't reached.

The user provided the HTML structure for the pagination buttons:

```html
<div class="vui_pagenation--btns">
  ...
  <button class="vui_button vui_pagenation--btn vui_pagenation--btn-side">下一页</button>
</div>
```

This confirms the selector for the "Next Page" button.

**Plan**:

1. **Update** **`core/selectors.py`**:

   * Add `next_page` selector to `SEARCH` dictionary.

   * Selector: `.vui_pagenation--btn-side:has-text("下一页"), button:has-text("下一页")`

2. **Update** **`core/search.py`**:

   * Refactor `search_videos` to be a generator or stateful method, OR (simpler for now) add a `next_page()` method.

   * Actually, `main.py` needs to control the loop.

   * I will add a `turn_page()` method to `SearchManager` that attempts to click the "Next Page" button and waits for the list to reload.

   * Update `search_videos` to optionally *not* navigate to the URL if we are already on the search page (to support just scraping current page).

   * *Refined approach*:

     * Modify `search_videos` to take a `page_num` argument? No, Bilibili URL pagination is easier: `&page={page}`.

     * Wait, the user wants to use the "Next Page" button logic (simulating user).

     * I will add `go_to_next_page()` in `SearchManager`.

     * I will add `scrape_current_page()` to extract videos from the *current* view.

     * `search_videos` will be the initial entry point.

3. **Update** **`main.py`**:

   * Implement the "Pagination Loop":

     * Start search.

     * Loop:

       * Scrape videos from current page.

       * Filter videos.

       * Process comments.

       * If `total_success >= target`, break.

       * If not enough, call `search_mgr.go_to_next_page()`.

       * If `go_to_next_page` fails (no button/disabled), break (end of results).

**Implementation Details**:

* **Selectors**: `.vui_pagenation--btns .vui_pagenation--btn-side` (check for text "下一页").

* **SearchManager**:

  * `search_videos(keyword, ...)` -> Initial navigation. Returns videos from page 1.

  * `get_current_page_videos()` -> Extracts videos from current DOM.

  * `next_page()` -> Clicks button, waits for loader/new content.

**Revised Plan Steps**:

1. Add selector.
2. Refactor `search.py` to separate "Navigation" from "Extraction".
3. Update `main.py` to use the new `next_page` capability in the loop.

