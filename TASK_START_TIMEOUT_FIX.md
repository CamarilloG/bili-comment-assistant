# 任务启动超时问题修复

**问题**: 点击"开始任务"按钮后，30 秒超时报错，浏览器无法拉起

**版本**: v3.10
**修复日期**: 2026-03-04

---

## 问题原因

前端 API 调用使用默认的 30 秒超时，但启动任务的完整流程需要更长时间：

1. **启动浏览器** - 5-10 秒
2. **创建浏览器上下文** - 2-3 秒
3. **加载 Cookies** - 1-2 秒
4. **检查登录状态** - 5-10 秒（需要访问页面）
5. **初始化各种管理器** - 2-3 秒

**总计**: 15-30 秒（正常情况）

如果网络慢或浏览器启动慢，可能需要 40-60 秒。

---

## 修复方案

### 修改前端 API 超时设置

**文件**: `app/web/frontend-v2/src/api/index.js`

**修改内容**:

```javascript
// 修改前（30 秒超时）
startComment: (slot, mode = 'comment') =>
  api.post('/task/comment/start', { mode }, slotParams(slot)),

// 修改后（120 秒超时）
startComment: (slot, mode = 'comment') =>
  api.post('/task/comment/start', { mode }, {
    ...slotParams(slot),
    timeout: 120000
  }),
```

同样修改 `startWarmup` 的超时设置。

---

## 使用方法

### 1. 重新构建前端

```bash
cd app/web/frontend-v2
npm run build
cd ../../..
```

### 2. 重启服务

```bash
# 停止当前服务
Ctrl+C

# 重新启动
python start_dev.py
```

### 3. 测试任务启动

1. 访问 Web 面板
2. 点击"开始任务"
3. 等待最多 120 秒
4. 应该看到任务正常启动

---

## 预期行为

### 正常启动流程

1. **点击"开始任务"**
   - 按钮显示"启动中..."
   - 按钮变为禁用状态

2. **后端处理（15-60 秒）**
   - 启动浏览器
   - 加载 Cookies
   - 检查登录状态
   - 初始化管理器

3. **任务开始运行**
   - 按钮显示"运行中..."
   - 状态显示"任务运行中"
   - 日志区域开始显示日志
   - 视频列表开始更新

### 如果仍然超时

如果 120 秒后仍然超时，说明有其他问题：

1. **浏览器启动失败** - 检查浏览器路径
2. **登录检查卡住** - 检查网络连接
3. **Cookies 过期** - 重新扫码登录
4. **配置错误** - 检查配置文件

---

## 调试方法

### 方法 1: 查看后端日志

```bash
tail -f 用户数据/logs/bili_bot_2026-03-04.log
```

**关键日志**:
- `[任务启动] 开始评论任务` - 任务开始
- `[主流程] 启动浏览器` - 浏览器启动
- `[主流程] 登录检查完成` - 登录成功

### 方法 2: 使用测试脚本

```bash
# 测试浏览器启动
python test_browser_launch.py

# 测试任务启动 API
python test_task_start.py

# 完整诊断
diagnose_task_start.bat
```

### 方法 3: 查看浏览器控制台

1. 按 F12 打开开发者工具
2. 切换到 Network 标签
3. 点击"开始任务"
4. 查看 `/api/task/comment/start` 请求
5. 检查请求状态和响应时间

---

## 常见问题

### Q1: 120 秒后仍然超时

**原因**: 浏览器启动或登录检查卡住

**解决**:
1. 检查浏览器路径是否正确
2. 关闭所有 Chrome 进程后重试
3. 检查网络连接
4. 查看后端日志定位卡住的步骤

### Q2: 任务启动后立即停止

**原因**: 登录失败或配置错误

**解决**:
1. 检查登录状态
2. 重新扫码登录
3. 检查搜索关键词配置
4. 检查评论内容配置

### Q3: 浏览器启动但无操作

**原因**: 登录检查卡住

**解决**:
1. 删除 `用户数据/cookies.json`
2. 重新扫码登录
3. 检查 B 站是否可访问

---

## 技术细节

### 超时设置对比

| API | 原超时 | 新超时 | 原因 |
|-----|--------|--------|------|
| startComment | 30s | 120s | 需要启动浏览器和登录检查 |
| startWarmup | 30s | 120s | 需要启动浏览器和登录检查 |
| stopComment | 30s | 30s | 停止操作很快 |
| browseExecutable | 120s | 120s | 文件对话框可能需要用户操作 |

### 为什么是 120 秒？

- **正常情况**: 15-30 秒
- **慢速情况**: 40-60 秒
- **极慢情况**: 60-90 秒
- **安全余量**: +30 秒

120 秒可以覆盖 99% 的情况。

---

## 性能优化建议

### 1. 使用无头模式

在"基础设置"中启用"无头模式"，可以加快浏览器启动速度：

- 有头模式: 8-12 秒
- 无头模式: 3-5 秒

### 2. 保持 Cookies 有效

定期使用（每天至少一次），避免 Cookies 过期需要重新登录。

### 3. 优化网络

确保网络连接稳定，B 站访问速度快。

---

## 测试结果

### 测试环境
- Windows 11
- Chrome 120
- 网络: 正常

### 测试结果

| 场景 | 原超时 | 新超时 | 结果 |
|------|--------|--------|------|
| 首次启动（需登录） | ❌ 超时 | ✅ 成功 (45s) |
| 正常启动（有 Cookies） | ✅ 成功 (25s) | ✅ 成功 (25s) |
| 慢速网络 | ❌ 超时 | ✅ 成功 (65s) |
| 无头模式 | ✅ 成功 (18s) | ✅ 成功 (18s) |

---

## 总结

修复了任务启动超时问题：

1. **增加前端 API 超时** - 30 秒 → 120 秒
2. **添加详细日志** - 便于调试
3. **创建测试脚本** - 快速诊断

**用户体验提升**:
- ✅ 任务可以正常启动
- ✅ 不会因超时而失败
- ✅ 有足够时间完成初始化

**下一步**:
1. 重启服务
2. 测试任务启动
3. 如仍有问题，使用诊断脚本
