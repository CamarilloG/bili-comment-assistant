# Bilibili Bot v3.7.1 更新日志

## 版本信息
- 版本号：v3.7.1
- 发布日期：2026-02-28
- 更新类型：Bug 修复

---

## 修复内容

### 🐛 Bug 修复

#### 1. 养号填充间隔状态不恢复 ✅

**问题描述：**
- 当评论间隔 ≥ 3 分钟时，会触发养号填充间隔
- 养号结束后，状态一直显示"养号填充间隔"
- 不会恢复为正常的"评论成功"状态

**影响范围：**
- 使用大间隔评论的用户
- 状态显示不准确，影响任务监控

**修复方案：**
- 在养号填充完成后，自动恢复状态为"评论成功"
- 添加日志："养号填充完成，继续评论任务"
- 确保状态回调正确更新

**修改文件：**
- `app/main.py` (第515-517行)

**修复代码：**
```python
# 养号结束后恢复正常状态
logger.info("养号填充完成，继续评论任务")
if status_callback:
    status_callback(video_info['bv'], "评论成功", comment_content=text, comment_type=comment_source)
```

---

## v3.7.0 功能回顾

### ✨ 主要功能

1. **非前台实例状态显示修复**
   - 所有实例的运行状态实时显示
   - 后台实例运行时正确显示"运行中"
   - 每3秒自动刷新状态

2. **高级模型调用优化**
   - 添加 `request_interval` 参数：控制调用间隔
   - 添加 `retry_delay_base` 参数：智能重试延迟
   - 支持指数退避重试策略

3. **License 验证整合**
   - 验证界面集成到主启动器
   - 不再弹出独立窗口
   - 自动查找并验证 license.lic

4. **前端路由修复**
   - 修复打包后前端 404 问题
   - 正确配置 `sys._MEIPASS/web` 路径

---

## 配置建议

### 针对不同模型的配置：

**高级模型（Gemini/GPT/Claude）：**
```yaml
ai:
  timeout: 60
  max_retries: 3
  request_interval: 3.0      # 3秒间隔
  retry_delay_base: 3.0      # 重试延迟：3s, 6s, 9s
```

**中级模型（Grok/Qwen）：**
```yaml
ai:
  timeout: 30
  max_retries: 2
  request_interval: 1.0      # 1秒间隔
  retry_delay_base: 2.0      # 重试延迟：2s, 4s
```

**快速模型（DeepSeek/OSS）：**
```yaml
ai:
  timeout: 30
  max_retries: 2
  request_interval: 0        # 无间隔限制
  retry_delay_base: 2.0      # 重试延迟：2s, 4s
```

---

## 升级说明

### 从 v3.7.0 升级到 v3.7.1

1. **无需修改配置文件**
   - 配置文件格式完全兼容
   - 新增的修复不影响现有配置

2. **重新打包（如需）**
   ```bash
   python -m PyInstaller launcher.spec --clean
   ```

3. **或使用便携版打包**
   ```bash
   build_portable.bat
   ```

---

## 已知问题

无

---

## 下一步计划

- 继续优化模型调用稳定性
- 增强多实例管理功能
- 改进日志显示和错误提示

---

## 技术细节

### 修复前的问题流程：
1. 评论成功 → 状态："评论成功"
2. 计算延迟 ≥ 3分钟 → 状态："养号填充间隔"
3. 养号运行中 → 状态："养号填充间隔"
4. 养号结束 → 状态：**仍然是"养号填充间隔"** ❌

### 修复后的正确流程：
1. 评论成功 → 状态："评论成功"
2. 计算延迟 ≥ 3分钟 → 状态："养号填充间隔"
3. 养号运行中 → 状态："养号填充间隔"
4. 养号结束 → 状态：**恢复为"评论成功"** ✅
5. 继续下一个视频 → 状态正常更新

---

## 感谢

感谢用户反馈的问题，帮助我们持续改进产品！
