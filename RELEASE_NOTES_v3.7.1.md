# Bilibili Bot v3.7.1 更新说明

## 版本信息
- 版本号：v3.7.1
- 更新日期：2026-02-28
- 更新类型：功能增强 + 体验优化

---

## 主要更新

### 🔔 全局通知系统（新功能）

实现了完整的通知系统，包括 Web 端吐司通知和 Windows 系统通知。

**功能特点：**
1. **Web 端吐司通知**
   - 任意实例检测到验证码时，前端显示吐司提示
   - 连续 3 次失败时显示警告通知
   - 任务终止时显示严重错误通知
   - 自动消失（普通 5 秒，重要 10 秒）
   - 支持手动关闭

2. **Windows 系统通知**
   - 仅重要事件触发（验证码 + 连续失败 + 任务终止）
   - 支持 plyer 和 win10toast 库
   - 自动降级（库不可用时静默失败）

3. **连续失败追踪**
   - 自动追踪每个实例的失败次数
   - 连续 3 次失败触发通知
   - 成功时自动重置计数

**通知类型：**
- 🔵 `info`: 普通信息
- 🟡 `warning`: 警告
- 🔴 `error`: 错误
- 🟠 `captcha`: 验证码（必须显示系统通知）
- 🔴 `critical`: 严重错误（必须显示系统通知）

---

## 技术实现

### 新增文件

1. **后端文件**
   - `app/core/notification_manager.py` - 通知管理器核心
   - `app/web/routers/notification_api.py` - WebSocket 通知端点

2. **前端文件**
   - `app/web/frontend-v2/src/components/ToastNotification.vue` - 吐司通知组件

3. **文档文件**
   - `NOTIFICATION_SYSTEM_v3.7.1.md` - 通知系统详细文档
   - `test_notifications.py` - 通知系统测试脚本

### 修改文件

1. **后端集成**
   - `app/main.py` - 集成通知调用
   - `app/web/app.py` - 注册通知路由

2. **前端集成**
   - `app/web/frontend-v2/src/App.vue` - 添加吐司组件，更新版本号

---

## 使用说明

### 前端使用

通知会自动显示在页面右上角，无需任何配置。

**通知示例：**
```
🟠 实例 0 检测到验证码
   今日第 1 次触发，将冷却 30 分钟
```

### 系统通知

Windows 系统通知会在以下情况自动弹出：
1. 检测到验证码
2. 连续 3 次失败
3. 任务终止

**依赖库：**
```bash
pip install plyer
# 或
pip install win10toast
```

### 测试通知

运行测试脚本验证通知系统：
```bash
cd app
python ../test_notifications.py
```

---

## 部署步骤

### 1. 安装依赖（可选）

```bash
pip install plyer
```

如果不安装，系统通知会静默失败，但 Web 通知仍然正常工作。

### 2. 前端已构建

前端已经重新构建，包含了新的吐司通知组件。

### 3. 重新打包启动器（如需）

```bash
python -m PyInstaller launcher.spec --clean
```

---

## 配置选项

### 失败计数阈值

在 `app/core/notification_manager.py` 中修改：

```python
if count >= 3:  # 修改这里的数字
    # 发送通知
```

### 通知持续时间

在 `ToastNotification.vue` 中修改：

```javascript
const duration = data.type === 'captcha' || data.type === 'critical' ? 10000 : 5000
// 修改这里的毫秒数（单位：毫秒）
```

### 禁用系统通知

在 `app/core/notification_manager.py` 中修改 `show_system` 参数：

```python
self.send_notification(
    title=title,
    message=message,
    notification_type="captcha",
    slot_id=slot_id,
    show_system=False,  # 改为 False 禁用系统通知
)
```

---

## 兼容性

### 浏览器兼容性
- Chrome/Edge: ✅ 完全支持
- Firefox: ✅ 完全支持
- Safari: ✅ 完全支持

### 系统兼容性
- Windows 10/11: ✅ 完全支持（系统通知）
- Windows 7/8: ⚠️ 部分支持（仅 Web 通知）
- macOS/Linux: ⚠️ 部分支持（仅 Web 通知）

---

## 已知问题

1. **系统通知依赖外部库**
   - 需要安装 plyer 或 win10toast
   - 如果都不可用，系统通知会静默失败（不影响 Web 通知）

2. **WebSocket 重连延迟**
   - 重连间隔固定为 3 秒
   - 可能导致短暂的通知丢失

3. **失败计数不持久化**
   - 重启服务后失败计数清零
   - 如需持久化，可使用数据库或文件

---

## 测试验证

### 测试场景 1: 验证码通知

1. 启动任意实例的评论任务
2. 触发验证码
3. **预期结果**：
   - ✅ 前端显示橙色吐司通知
   - ✅ Windows 系统通知弹出
   - ✅ 通知持续 10 秒后自动消失

### 测试场景 2: 连续失败通知

1. 启动任意实例的评论任务
2. 连续失败 3 次
3. **预期结果**：
   - ✅ 前端显示红色吐司通知
   - ✅ Windows 系统通知弹出
   - ✅ 显示失败次数和原因

### 测试场景 3: 多实例通知

1. 实例 0 和实例 1 同时运行
2. 实例 0 触发验证码
3. **预期结果**：
   - ✅ 所有打开的前端页面都显示通知
   - ✅ 通知明确标注"实例 0"

---

## 升级说明

### 从 v3.7.0 升级

1. **无需修改配置文件**
2. **无需修改数据库**
3. **建议安装 plyer 库**（可选）
4. **重新打包启动器**（如果使用打包版本）

### 数据迁移

无需数据迁移，通知系统是全新功能。

---

## 未来计划

### v3.7.2 可能的改进：

1. **通知历史记录**
   - 保存最近 100 条通知
   - 提供查看历史的界面

2. **通知过滤**
   - 允许用户选择接收哪些类型的通知
   - 静音特定实例的通知

3. **声音提示**
   - 重要通知播放提示音
   - 可配置音量和音效

4. **远程推送**
   - 集成 Server 酱、PushPlus
   - 支持邮件/微信通知

---

## 反馈与支持

如有问题或建议，请通过以下方式反馈：
- GitHub Issues
- 项目讨论区

---

## 致谢

感谢所有用户的反馈和建议，帮助我们不断改进产品。

---

## 版本历史

### v3.7.1 (2026-02-28)
- ✨ 新增全局通知系统
- ✨ 新增 Web 端吐司通知
- ✨ 新增 Windows 系统通知
- ✨ 新增连续失败追踪
- 🐛 修复实例切换延迟问题（v3.7.0）
- 🐛 修复实例状态显示错误（v3.7.0）
- 🐛 修复实例配置共用问题（v3.7.0）

### v3.7.0 (2026-02-27)
- ✨ 新增多实例支持
- ✨ 新增实例切换器
- ✨ 新增 AI 模型调用优化
- 🐛 修复养号状态不恢复问题
- 🐛 修复 GUI 启动器双开浏览器

### v3.6.0 (2026-02-26)
- ✨ 新增 GUI 启动器
- ✨ 新增 License 验证
- ✨ 新增 Web 面板
