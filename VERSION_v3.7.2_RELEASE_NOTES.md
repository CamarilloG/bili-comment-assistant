# Bilibili Bot v3.7.2 版本更新说明

## 版本信息

- **版本号**: v3.7.2
- **发布日期**: 2026-02-28
- **更新类型**: 严重 Bug 修复

---

## 更新内容

### 1. 修复停止按钮无响应问题 ✅

**问题描述**：
- 点击停止按钮后需要等待很久才能停止任务
- 可能需要等待 30 秒到几分钟不等

**修复方案**：
- 在所有长时间操作前后增加停止信号检查点
- 将长时间等待分割成 1 秒间隔的可中断等待
- 每秒检查一次停止信号

**修复效果**：
- 停止按钮现在可以在 1-2 秒内快速响应 ✅
- 无论任务处于什么状态都能快速停止

---

### 2. 修复浏览器关闭导致系统卡死问题 ✅

**问题描述**：
- 手动关闭浏览器后，程序开始无限重试
- 每次操作都失败，不断重试
- CPU 占用 100%，内存暴涨
- 最终导致系统卡死崩溃

**修复方案**：
- 添加 PlaywrightError 异常捕获
- 添加浏览器连接检查函数
- 在关键位置检查浏览器是否已关闭
- 检测到浏览器关闭后立即终止任务

**修复效果**：
- 浏览器关闭后任务立即终止 ✅
- 不会无限重试
- 不会导致系统卡死

---

### 3. 新增连续失败保护机制 ✅

**功能描述**：
- 记录每个操作的连续失败次数
- 超过 5 次连续失败后自动停止任务
- 发送通知告知用户

**保护效果**：
- 防止网络故障等问题导致无限重试
- 及时发现并停止异常任务
- 保护系统资源

---

### 4. 优化停止信号检查点 ✅

**优化内容**：
- 在关键词循环前检查
- 在视频循环前检查
- 在搜索操作前检查
- 在 AI 操作前检查
- 在评论发布前检查
- 在等待期间每秒检查

**优化效果**：
- 全方位覆盖所有可能的阻塞点
- 确保停止信号能够快速响应

---

### 5. 优化等待机制 ✅

**优化内容**：
- 将 `stop_ev.wait(delay)` 替换为 `interruptible_wait(stop_ev, delay)`
- 长时间等待分割成 1 秒间隔
- 每秒检查一次停止信号

**优化效果**：
- 验证码静默等待可中断
- 评论间隔延迟可中断
- 所有等待都能快速响应停止信号

---

### 6. 用户数据目录优化 ✅ 🆕

**变更说明**：
- 所有配置文件、日志文件和用户数据现在存储在软件根目录下的 **"用户数据"** 文件夹
- 不再使用临时文件或 app 目录

**新的目录结构**：
```
软件根目录/
└── 用户数据/
    ├── config.yaml           # 实例 0 配置
    ├── cookies.json          # 实例 0 Cookie
    ├── history.json          # 实例 0 历史
    ├── comment_log.csv       # 实例 0 日志
    ├── captcha_record.json   # 验证码记录
    ├── logs/                 # 日志文件夹
    └── instances/            # 多实例数据
        ├── 1/
        ├── 2/
        └── ...
```

**优势**：
- 数据集中管理，便于备份和迁移
- 避免权限问题和临时文件被清理
- 用户数据与程序分离，更新不影响数据
- 支持多实例独立存储

**迁移指南**：
- 详见 `USER_DATA_MIGRATION_v3.7.2.md` 文档
- 首次启动会自动创建 `用户数据` 文件夹
- 需要手动迁移旧数据（从 `app/` 目录复制到 `用户数据/`）

---

## 技术细节

### 新增函数

1. **check_retry_limit(slot_id, operation)** - 检查重试上限
2. **reset_retry_count(slot_id, operation)** - 重置重试计数
3. **is_browser_connected(browser)** - 检查浏览器连接
4. **interruptible_wait(stop_event, seconds)** - 可中断等待

### 新增常量

- **MAX_CONSECUTIVE_FAILURES = 5** - 最大连续失败次数

### 修改文件

1. **app/main.py** - 主要修复文件
   - 导入 PlaywrightError
   - 添加重试计数器
   - 添加浏览器连接检查
   - 添加停止信号检查点
   - 使用可中断等待
   - 添加异常捕获

2. **app/web/frontend-v2/src/App.vue** - 更新版本号到 v3.7.2

3. **build_portable.bat** - 更新到 v3.7.2
4. **build_launcher_debug.bat** - 更新到 v3.7.2
5. **build_web_exe.bat** - 更新到 v3.7.2
6. **launcher.spec** - 更新到 v3.7.2
7. **launcher_debug.spec** - 创建调试版 spec
8. **run_web.spec** - 更新到 v3.7.2

---

## 打包说明

### 打包脚本

1. **build_portable.bat** - 打包完整便携版（推荐）
   - 输出：`dist\BiliBot_v3.7.2_Portable\`
   - 包含：启动器 + Python 环境 + 配置文件

2. **build_launcher.bat** - 仅打包启动器
   - 输出：`dist\BiliBotLauncher_v3.7.2.exe`

3. **build_launcher_debug.bat** - 打包调试版启动器
   - 输出：`dist\BiliBotLauncher_v3.7.2_debug.exe`
   - 带控制台窗口，方便查看错误

4. **build_web_exe.bat** - 打包 Web 单文件版
   - 输出：`dist\BiliBot_Web_v3.7.2.exe`

### 打包步骤

1. 确保前端已构建：
   ```bash
   cd app/web/frontend-v2
   npm run build
   ```

2. 运行打包脚本：
   ```bash
   build_portable.bat
   ```

3. 将 `license.lic` 放入输出目录

4. 打包整个目录分发给用户

---

## 升级指南

### 从 v3.7.1 升级到 v3.7.2

1. **备份数据**：
   - 备份 `config.yaml`
   - 备份 `cookies.json`
   - 备份 `history.json`

2. **替换文件**：
   - 替换 `BiliBotLauncher_v3.7.2.exe`
   - 替换 `python/` 目录（如果有更新）

3. **恢复数据**：
   - 恢复备份的配置文件

4. **启动测试**：
   - 启动程序
   - 测试停止按钮
   - 测试浏览器关闭处理

---

## 测试验证

### 测试 1: 停止按钮响应速度

1. 启动评论任务
2. 点击停止按钮
3. **预期**：1-2 秒内任务停止 ✅

### 测试 2: 浏览器关闭处理

1. 启动评论任务
2. 手动关闭浏览器窗口
3. **预期**：
   - 检测到浏览器关闭
   - 日志显示："浏览器已关闭，任务终止"
   - 任务立即停止
   - 不会重启浏览器 ✅

### 测试 3: 连续失败保护

1. 启动评论任务
2. 模拟网络故障（断网）
3. **预期**：
   - 连续失败 5 次后停止重试
   - 发送通知
   - 任务终止 ✅

### 测试 4: 长时间等待中断

1. 启动评论任务
2. 触发验证码（进入静默等待）
3. 在等待期间点击停止
4. **预期**：1-2 秒内停止 ✅

---

## 已知问题

无

---

## 下一步计划

- 优化 AI 调用性能
- 增强养号行为多样性
- 添加更多风控检测机制

---

## 联系方式

如有问题请反馈：
- GitHub Issues
- 技术支持群

---

## 致谢

感谢所有用户的反馈和支持！

---

**重要提示**：
- 本次更新修复了两个严重的系统稳定性问题
- 强烈建议所有用户升级到 v3.7.2
- 升级前请备份重要数据
